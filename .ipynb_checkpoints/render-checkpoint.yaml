services:
  - type: web
    name: app
    env: python3.10
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -b :10000 app:app
    envVars:
      - key: PORT
        value: 10000