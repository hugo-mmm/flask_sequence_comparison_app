services:
  - type: web
    name: flask-app
    env: python3.10
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -b :10000 app:application
    envVars:
      - key: PORT
        value: 10000