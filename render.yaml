services:
- type: web
  name: flask-app
  env: python
  buildCommand: |
    pip install -r requirements.txt
    pip install --upgrade pip
  startCommand: gunicorn app:app -b :10000
  envVars:
  - key: PORT
    value: 10000